{
  "name": "ulab",
  "version": "3.10.0",
  "private": true,
  "description": "ULAB — Universal Local AI Bridge",
  "workspaces": [
    "desktop/renderer",
    "desktop",
    "agent",
    "shared"
  ],
  "scripts": {
    "agent:exe": "npm --prefix agent run build:exe",
    "website:check": "node scripts/verify-website.cjs",
    "renderer:typecheck": "npm --prefix desktop/renderer run typecheck",
    "renderer:permission-check": "npx tsx desktop/renderer/src/components/PermissionCenter.test.tsx",
    "renderer:build": "npm --prefix desktop/renderer run build",
    "agent:build": "npm --prefix agent run build",
    "agent:test": "npm --prefix agent test",
    "desktop:check": "npm --prefix desktop run check",
    "verify": "npm run website:check && npm run renderer:typecheck && npm run renderer:permission-check && npm run renderer:build && npm run agent:build && npm run agent:test && npm run desktop:check",
    "desktop:build": "npm run renderer:build && npm run agent:build && npm run agent:exe && npm run desktop:check && npx electron-builder --config desktop/electron-builder.yml --win nsis"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^44.4.3",
    "electron-builder": "^26.15.3",
    "tsx": "^4.23.15",
    "wait-on": "^8.0.4"
  }
}
